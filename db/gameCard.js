let db = require('./connection');

class Card extends ActiveRecord {
  id

  shuffle(){
  }

  reshuffle() {
  }

  // give card to player from draw pile
  // return the card
  drawCard(player) {
  }
}
